/**
 * Legacy User model — retained for backward compatibility with WebApiCallsService.
 * Not used in the Semantic Layer feature.
 */
export interface User {
  id?: number;
  name?: string;
  username?: string;
}
